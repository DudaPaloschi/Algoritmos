function calculaArea(altura, largura) { //dentro desses parenteses são parâmetros
    const area = altura * largura
    console.log(area)

}

calculaArea (2, 3 )//dentro desses parenteses são argumentos